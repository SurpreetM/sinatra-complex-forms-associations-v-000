<h1>Update Pet</h1>
<br>
<form action ="/pets/<%=@pet.id%>" method="post">

  Name:<input type="text" value="<%=@pet.name%>" id="pet_name" name="pet[name]">
<br>
  Choose an existing owner:
<br>
  <%Owner.all.each do |owner|%>
    <input type="radio" name="pet[owner_id]" value="<%=owner.id%>" id="<%=owner.name%>"
    <%='checked' if @pet.owner.name == owner.name %>><%=owner.name%></input>
    <%end%>
<br>
  and/or create a new owner:
<br>
  <input type="text" name="owner[name]" id="owner_name"></input>
<br>
  <input type ="submit" value="Update Pet"

</form>
